diff --git a/rules.mk b/rules.mk
index d9b6b71f..dc12f64f 100644
--- a/rules.mk
+++ b/rules.mk.desired
@@ -14,11 +14,19 @@ MODULE_RUSTFLAGS += \
 	--cfg 'feature="alloc"' \
 	--cfg 'feature="default"' \
 	--cfg 'feature="race"' \
-	--cfg 'feature="std"'
+
+ifeq ($(call TOBOOL,$(TRUSTY_USERSPACE)),true)
+
+MODULE_RUSTFLAGS += \
+	--cfg 'feature="std"' \
+
+else

 MODULE_LIBRARY_DEPS := \
 	trusty/user/base/lib/liballoc-rust \
 	trusty/user/base/lib/libcompiler_builtins-rust \
 	trusty/user/base/lib/libcore-rust

+endif
+
 include make/library.mk
