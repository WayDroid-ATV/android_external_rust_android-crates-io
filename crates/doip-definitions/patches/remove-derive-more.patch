diff --git a/Cargo.toml b/Cargo.toml
index 37923c9d9..2c685f929 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -61,10 +61,6 @@ crate-type = [
 ]
 path = "src/lib.rs"
 
-[dependencies.derive_more]
-version = "2.0.1"
-features = ["from"]
-
 [dependencies.pyo3]
 version = "0.24.2"
 features = [
diff --git a/src/error.rs b/src/error.rs
index 62d98be95..78c871c89 100644
--- a/src/error.rs
+++ b/src/error.rs
@@ -1,5 +1,3 @@
-use derive_more::From;
-
 /// Custom Result type which allows for easier typing of errors across the API.
 pub type Result<T> = core::result::Result<T, Error>;
 
@@ -7,7 +5,7 @@ pub type Result<T> = core::result::Result<T, Error>;
 ///
 /// Expand to fit new dependencies using `#[from]` and implement custom error types
 /// with context.
-#[derive(Debug, From)]
+#[derive(Debug)]
 pub enum Error {
     /// When utilising the builder pattern the payload is not present
     PayloadNotBuilt,
@@ -98,8 +96,7 @@ pub enum Error {
         size: usize,
     },
 
-    /// Derived implementation for standard library IO errors
-    #[from]
+    /// Standard library IO errors
     #[allow(clippy::enum_variant_names)]
     SliceError(core::array::TryFromSliceError),
 }
@@ -109,3 +106,9 @@ impl core::fmt::Display for Error {
         write!(fmt, "{self:?}")
     }
 }
+
+impl From<core::array::TryFromSliceError> for Error {
+    fn from(err: core::array::TryFromSliceError) -> Self {
+        Error::SliceError(err)
+    }
+}
